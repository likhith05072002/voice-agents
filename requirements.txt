fastapi==0.115.5
uvicorn==0.32.1
websockets==14.2
httpx==0.28.0
structlog==24.4.0
pydantic==2.9.0
# BaseSettings moved out of pydantic core in v2 — config.py imports it from here.
pydantic-settings==2.6.1
numpy==1.26.4
uvloop==0.21.0; sys_platform != "win32"
# Python 3.13 removed the stdlib `audioop` (PEP 594); this backport restores it.
# The whole telephony path (mulaw encode/decode, ratecv, rms) imports audioop.
audioop-lts==0.2.1; python_version >= "3.13"
# Accounts mode (DATABASE_URL set): Postgres pool + OAuth state signing.
asyncpg==0.30.0
itsdangerous==2.2.0
pypdf==5.1.0
# Ed25519 verification of Telnyx webhook signatures (src/security/telnyx.py).
# Without it the verifier fails CLOSED and every webhook is rejected.
PyNaCl==1.5.0
